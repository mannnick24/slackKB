{
  "name": "slackkb",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix api && npm run build --prefix web",
    "image": "docker build -f api/Dockerfile -t slackkb-api:latest . && docker build -f web/Dockerfile -t slackkb-web:latest .",
    "image:api": "docker build -f api/Dockerfile -t slackkb-api:latest .",
    "image:web": "docker build -f web/Dockerfile -t slackkb-web:latest .",
    "startApi": "npm run start --prefix api",
    "startWeb": "npm run dev --prefix web",
    "start": "concurrently -k -n api,web -c blue,green \"npm run start --prefix api\" \"npm run preview --prefix web\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
